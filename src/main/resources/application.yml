spring:
  datasource:
    url: jdbc:mysql://localhost:3306/UsEarth # 본인 스키마로 변경
    username: root
    password: 1234 # 비번도 바꿔주세요
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true